    <h1>Översikt</h1>

    <section class="half-section list">
      <h1><a href="<?php echo urlModr('page'); ?>">Sidor och undersidor</a></h1>

      <p>Senast redigerade:</p>

      <ul class="shortcuts">
<?php foreach ($last_pages as $page): ?>
        <li>
          <a href="<?php echo urlModrSite($page->url); ?>"><?php echo htmlspecialchars($page->title); ?></a> 
          (<?php echo easyDate('H:i', $page->edited) . ' av ' . htmlspecialchars($page->name); ?>) 
          <a href="<?php echo urlModr('edit-page', 'show', $page->url); ?>" class="clink edit-link">Redigera</a>
        </li>
<?php endforeach; ?>
      </ul>
    </section>

    <section class="half-section list">
      <h1><a href="<?php echo urlModr('controllers'); ?>" class="clink">Moduler</a></h1>
<?php if (count($controllers)): ?>
      <p>Dina favoritmoduler:</p>

      <ul class="shortcuts">
<?php foreach ($controllers as $controller): ?>
        <li><a href="<?php echo urlModr($controller->class_name); ?>" class="clink"><?php echo htmlspecialchars($controller->name); ?></a></li>
<?php endforeach; ?>
      </ul>
<?php endif; ?>
    </section>

<?php foreach ($controllers as $controller):
   if ($controller->has_favorite_config): ?>
    <section class="half-section">
<?php require('controller.favorite.' . $controller->class_name . '.phtml'); ?>
    </section>

<?php endif;
endforeach; ?>
